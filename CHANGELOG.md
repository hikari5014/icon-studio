# 更新日誌

App 裡「版本與更新」那一頁顯示的是 `js/changelog.js`（寫給使用者看的白話版）。
這一份是完整的技術紀錄。

## v1.0.0 — 2026-09-22

第一版。

- 圖層系統：路徑／方形／圓形／曲線四種型別，可疊、可排序、可隱藏、可鎖定
- 鋼筆工具：逐點放樣，點回起點或 Enter 收尾，Shift 放尖角
- 錨點編輯：拖曳、雙擊插點、Del 刪點、方向鍵微調、平滑／尖角切換
- 描圖底圖：匯入 JPG／PNG／GIF／WebP，可調透明度、縮放、位移；不會被匯出
- SVG 匯入：path／rect／circle／ellipse／line／polygon／polyline，自動等比置中
- 四個資料庫：配色 50、造型 20、動畫 18、範例 24
- 外框三模式（自動／全部有／全部無），共用輪廓而非逐件描邊
- 時間軸可把動畫凍結在任意毫秒
- 匯出 SVG／CSS／React／Vue／HTML／PNG／設計檔 JSON
- PWA：可安裝、離線可用、版本偵測與一鍵更新
- 上線前結構檢查（版本一致性、SW 快取清單、ES module 語法）

### 開發過程中修掉的自種 bug（都是「不會報錯」那一類）

- **拖曳完全沒作用**：pointermove 掛在控制點元素上，而拖曳每一幀都會重建畫布
  （innerHTML），那顆元素被刪掉、監聽跟著消失。症狀是「點得到、選得起來、
  就是拖不動」，畫面上零錯誤。改成掛在 window 上。
- **整支模組當場停掉**：`clone` 在 `const` 宣告之前就被使用（暫時性死區）。
  HTML 殼照樣畫出來，看起來只是「沒有內容」——只有主控台看得到。
  修法是把工具函式移到狀態宣告之前。
- **檢查腳本自己報假錯**：用「剝掉 import/export 再 new Function」驗語法，
  會把 `export const X = {` 的開頭剝掉而留下孤兒 `}`，四個檔全部誤判語法錯誤。
  改成把檔案複製成 `.mjs` 交給 `node --check` 用真的 parser 驗。
